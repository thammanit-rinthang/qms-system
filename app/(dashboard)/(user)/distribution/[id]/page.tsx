// User-facing alias: the same authenticated detail view is available from
// both /distribution/[id] and /qms/distribution/[id].
export { default, metadata } from "@/app/(dashboard)/qms/distribution/[id]/page";
